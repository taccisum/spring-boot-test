spring:
  datasource:
    username: sa
    password: sa
    schema: classpath:schema-hsql.sql
    data: classpath:data-hsql.sql
    url: jdbc:hsqldb:mem:hsql_db
  output:
    ansi:
      enabled: always

server:
  port: 9090
